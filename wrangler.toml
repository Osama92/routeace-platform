name = "routeace-platform"
compatibility_date = "2025-01-01"
workers_dev = false
preview_urls = false

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# Environment variables are passed at build time via GitHub Actions.
# Do NOT put secret keys here — set them in the Cloudflare Pages dashboard
# under Settings → Environment Variables, or pass via wrangler secret.
